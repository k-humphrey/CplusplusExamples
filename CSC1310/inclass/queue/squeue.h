#ifndef SQUEUE_H
#define SQUEUE_H
    #include <iostream>
    using namespace std;

    class squeue{
        private:
                //girl

        public:
            squeue(){

            }
            //add
            // rear = (rear - 1) % queuesize
            //remove
            // front = (front + 1) % queuesize


    };
#endif